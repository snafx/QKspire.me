   function countChar(val) {
       var len = val.value.length;
       if (len >= 1000) {
           val.value = val.value.substring(0, 1000);
       } else {
           var char = 1 + len;
           $('#charNum').text(char + '/1000');
       }
   };
   function countChar2(val) {
       var len = val.value.length;
       if (len >= 1000) {
           val.value = val.value.substring(0, 1000);
       } else {
           var char = 1 + len;
           $('#charNum2').text(char + '/1000');
       }
   };
   function countChar3(val) {
       var len = val.value.length;
       if (len >= 1000) {
           val.value = val.value.substring(0, 1000);
       } else {
           var char = 1 + len;
           $('#charNum3').text(char + '/1000');
       }
   };
   function countChar4(val) {
       var len = val.value.length;
       if (len >= 1000) {
           val.value = val.value.substring(0, 1000);
       } else {
           var char = 1 + len;
           $('#charNum4').text(char + '/1000');
       }
   };
   function countChar5(val) {
       var len = val.value.length;
       if (len >= 1000) {
           val.value = val.value.substring(0, 1000);
       } else {
           var char = 1 + len;
           $('#charNum5').text(char + '/1000');
       }
   };
